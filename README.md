# Programming 수업 소스코드
## 02_counter
vanilla HTML, JavaScrpit
## React 시작😪😪😪
```shell
npm create vite@latest .
```
### CounterAPP😒
- `useState()`
- `onClick={() => set함수()}`
- `onClick={() => set함수((이전state) => dlwjs state + 1)}`
### TodoListApp 😪